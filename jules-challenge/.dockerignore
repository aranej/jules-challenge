# Node.js
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (if any for backend, usually not directly in src)
dist
build
out

# IDE specific
.vscode/
.idea/

# OS specific
.DS_Store
Thumbs.db

# Environment files (should be provided at runtime, not built into image)
.env
*.env

# Frontend project (if backend Docker context is the root jules-challenge dir)
frontend-vite/

# Git
.git
.gitignore

# Docker
Dockerfile
Dockerfile.*
docker-compose.yml
docker-compose.*.yml
